from surfaces import *

'''
Usage:
parabola(strategy, dxInput, noise, gpflag) If strategy is gpal, dxInput is ignored. If strategy is not GPAL, gpflag is ignored. gpflag is local by default.
townsend(strategy, dxInput, noise, gpflag)
lunar(strategy, dxInput, r_disp, gpflag)
'''
lunar("snake", "4", "6", "NN")
